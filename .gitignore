node_modules
.next
.pnpm-store
out
build
coverage
# Mutation testing (Stryker) — generated reports + temp
.stryker-tmp/
reports/
.env*.local
*.tsbuildinfo
tsconfig.tsbuildinfo
.DS_Store
platform-dev

# --- Claude Code (local agent state: worktrees, settings, checkpoints) ---
.claude/
