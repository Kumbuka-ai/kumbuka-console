Screenshot slots for the chatgpt-web cell, language-bound: <locale>/step-<id>.png
(de/ and en/; id = step number, optional letter suffix for two images on
one step). PNG 2x, tight crop, accent #FF5B1F marker, no secrets in the
picture. A missing image renders as a placeholder - never the other
language. Images provided by the maintainers.
